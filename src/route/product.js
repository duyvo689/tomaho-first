const express = require('express')
const router = express.Router()
const newController = require('../controller/controller')

//[GET] '/creates'
// router.use('/creates', newController.create)

module.exports = router